import 'package:flutter/material.dart';

class AppTheme {
  static final brightTheme = ThemeData.light().copyWith(
    // define all your app theme for light mode
  );
  static final darkTheme = ThemeData.dark().copyWith(
    // define all your app theme for dark mode
  );
}
